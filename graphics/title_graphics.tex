\documentclass[12pt]{standalone}

\usepackage{tikz}
\usepackage{amsmath}
\usepackage{unicode-math}
\input{../utils/colors}

\begin{document}

\usetikzlibrary{calc}

\begin{tikzpicture}[
    thick,
    arr/.style={ultra thick, draw=darkBlue},
    box/.style={rectangle, minimum width=1.8cm, minimum  height=1cm, draw=black},
    cross/.style={path picture={
            \draw[black]
            (path picture bounding box.south east) -- (path picture bounding box.north west) (path picture bounding
            box.south west)
            -- (path picture bounding box.north east);
          }},
    sum/.style={circle, minimum size=.75cm, draw=black, cross},
    ma/.style={midway, above},
    ml/.style={midway, left},
  ]
  \node[sum] (sum) at (0, 0) {};
  \node[box] (wc) at (2.25,0) {$W_c(s)$};
  \node[box] (wp) at (5.5,0) {$W_p(s)$};
  \node[box] (wv) at (3.875,-2) {$W_v(s)$};

  \draw[arr, to-] (sum.west) -- ++(-1.8,0) node[ma] {$U(s)$};
  \draw[arr, -to] (sum) -- (wc);
  \draw[arr, -to] (wc) -- (wp);
  \draw[arr, -to] (wp.east) -- ++(2.2,0) node[ma] {$Y(s)$};
  \draw[arr, -to] ($(wp.east)+(1.2,0)$) |- (wv);
  \draw[arr, -to] (wv) -| (sum);

  \node[circle, minimum size=5pt, fill=darkBlue, inner sep=0] at ($(wp.east)+(1.2,0)$) {};
  \node[above left=-3pt] at (sum.160) {$+$};
  \node[below left=-3pt] at (sum.250) {$-$};
\end{tikzpicture}
\end{document}
