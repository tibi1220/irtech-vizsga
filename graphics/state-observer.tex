\documentclass{standalone}

\usepackage{tikz}
\usepackage{amsmath}
\usepackage{unicode-math}
\input{../utils/colors}

\newcommand{\rvec}[1]{\mathbfit{#1}}
\newcommand{\hvec}[1]{\hat{\mathbfit{#1}}}
\newcommand{\rmat}[1]{\mathbf{#1}}

\begin{document}

\usetikzlibrary{calc}

\begin{tikzpicture}[
    thick,
    arr/.style={ultra thick, draw=darkBlue},
    err/.style={ultra thick, draw=darkRed},
    box/.style={
        rectangle,
        minimum width=1.4cm,
        minimum  height=1.1cm,
        draw=black,
        align=center,
        fill=yellow!5,
      },
    cross/.style={path picture={
            \draw[black]
            (path picture bounding box.south east) -- (path picture bounding box.north west) (path picture bounding
            box.south west)
            -- (path picture bounding box.north east);
          }},
    sum/.style={circle, minimum size=.75cm, draw=black, cross, fill=yellow!5},
    ma/.style={midway, above},
    mb/.style={midway, below},
    ml/.style={midway, left},
  ]
  \node[box, minimum width=2.25cm] (S) at (0,0) {rendszer};
  \node[box, minimum width=2.25cm] (O) at (0,-1.5) {megfigyelő};

  \node[circle, minimum size=5pt, fill=darkBlue, inner sep=0]
  (H1) at ($(S.west)-(.75,0)$) {};
  \node[circle, minimum size=5pt, fill=darkBlue, inner sep=0]
  (H2) at ($(S.east)+(.75,0)$) {};

  \draw[arr,to-] (S.west) -- ++(-1.5,0) node[ma] {$\rvec u(t)$};
  \draw[arr,-to] (S.east) -- ++(1.5,0) node[ma] {$\rvec y(t)$};
  \draw[arr,-to] (O.-20) -- ++(1.5,0) node[ma] {$\hvec x(t)$};

  \draw[arr,-to] (H1.center) |- (O);
  \draw[arr,-to] (H2.center) |- (O.20);
\end{tikzpicture}
\end{document}
