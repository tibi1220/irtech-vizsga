\documentclass{standalone}

\usepackage{tikz}
\usepackage{amsmath}
\usepackage{unicode-math}
\input{../utils/colors}

\begin{document}

\usetikzlibrary{calc}

\begin{tikzpicture}[
    thick,
    arr/.style={ultra thick, draw=darkBlue},
    err/.style={ultra thick, draw=darkRed},
    box/.style={
        rectangle,
        minimum width=1.5cm,
        minimum  height=1cm,
        draw=black,
        align=center,
        fill=yellow!5,
      },
    cross/.style={path picture={
            \draw[black]
            (path picture bounding box.south east) -- (path picture bounding box.north west) (path picture bounding
            box.south west)
            -- (path picture bounding box.north east);
          }},
    sum/.style={circle, minimum size=.75cm, draw=black, cross, fill=yellow!5},
    ma/.style={midway, above},
    mb/.style={midway, below},
    ml/.style={midway, left},
  ]
  \node[sum] (sum) at (0, 0) {};
  \node[box] (wc) at (2.5,0) {$W_c(s)$};
  \node[box] (wp) at (5.5,0) {$W_p(s)$};
  \node[box] (wv) at (4,-2) {$W_v(s) \equiv 1$};

  \draw[arr, to-] (sum.west) -- ++(-2,0) node[ma] {$R(s)$} ;
  \draw[arr, -to] (sum) -- (wc) node[ma] {$E(s)$} ;
  \draw[arr, -to] (wc) -- (wp) node[ma] {$U(s)$} ;
  \draw[arr, -to] (wp.east) -- ++(2,0) node[ma] {$Y(s)$} ;
  \draw[arr, -to] ($(wp.east)+(1,0)$) |- (wv);
  \draw[arr, -to] (wv) -| (sum);

  \node[circle, minimum size=5pt, fill=darkBlue, inner sep=0] at ($(wp.east)+(1,0)$) {};
  \node[above left=-3pt] at (sum.160) {$+$};
  \node[below left=-3pt] at (sum.250) {$-$};
\end{tikzpicture}
\end{document}
